Review the provided codebase for my chord and hand-modeling application. Currently, the system does not distinguish between different chord inversions.

Analyze the existing architecture and assess the work required to implement the following requirements:

1. Inputs:
- Add the ability for the system to evaluate and include chord inversions.
- Allow the user to optionally specify a desired (relative) inversion when requesting a chord.

2. Outputs:
- Update the data model and UI layers so that the relative inversion is always calculated, tracked, and displayed whenever chord voicings are rendered.

Based on your analysis of the codebase, generate a comprehensive update specifically formatted for the v0.5.4 design document. Your output must include:
- A technical assessment of the necessary changes to the data models, parsing logic, scoring systems, and UI components.
- A step-by-step implementation plan.
- The exact specifications and architecture updates to be inserted directly into the v0.5.4 design document.
